import { start } from "./start";
import { defineConfig } from "./options";
import { addActions } from "./actions";
import { makeAnim } from "./makeAnim";

export default {
  start,
  defineConfig,
  addActions,
  makeAnim,
};
